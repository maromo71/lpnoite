#include <stdio.h>
#define T 5
#define MAX 10
/**
 *  vetorA[T] = 5 valores unicos entrados pelo usuario
 *  vetorB[T] = 5 valores unicos entrados pelo usuarop
 *  vetorInt[T] = interseccao dos dois vetores
 *  vetorUni[MAX] = uniao dos valores (mantendo unicidade)
 */
int main(void) {
    int vetorA[T] = {0};
    int vetorB[T] = {0};
    int vetorInt[T] = {0};
    int vetorUni[MAX] = {0};

    return 0;
}