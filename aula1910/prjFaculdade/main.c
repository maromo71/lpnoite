#include <stdio.h>
#include "aluno.h"
int main(void)
{
    //inicializar
    inicializa();
    int pos = 0; //posicao inicial do vetor
    int opc = 0;
    do {
            printf("Menu Principal \n");
    }while(opc != 9);
    return 0;
}
